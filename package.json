{
  "name": "resumegpt_v2",
  "version": "1.0.0",
  "description": "Second rev of resumegpt",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "deploy-client": "cd client/ && flyctl deploy",
    "deploy-server": "cd server/ && flyctl deploy"
  },
  "author": "Mitchell Hay",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
